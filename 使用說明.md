# LYS 網站後台 — 使用說明

## 一、怎麼啟動（每天要做的事只有這一件）

在 `lys` 這個資料夾裡打開終端機（或在資料夾網址列打 `cmd` 按 Enter），輸入：

```
node server.js
```

看到「LYS 網站已啟動」就是成功了。然後開瀏覽器：

- **前台（訪客看到的網站）**：http://localhost:3000/
- **後台（你管理用的）**：http://localhost:3000/admin/

後台密碼：`lys2026`

> 想關掉伺服器：在終端機按 `Ctrl + C`。
> 想改密碼：用記事本開 `server.js`，第 18 行 `ADMIN_PASSWORD` 後面的字改掉，存檔重啟。

---

## 二、後台能做什麼

登入後有三個頁籤：

### 1. 作品管理
- 點「＋ 新增作品」上架新作品
- 點「編輯」改價格、已售數量、分類、介紹
- 點「刪除」下架作品
- 圖片可以**直接上傳**（點「選擇檔案」），不用管路徑
- 「已售出」數字大於等於「總版數」時，前台會自動顯示「售完」

### 2. 展覽管理
- 新增／編輯展覽：名稱、地點、日期、展出作品、圖片
- **狀態**很重要：選「展出中」才會出現在網站首頁

### 3. 藝術家申請
- 訪客在網站上填的投稿表單，資料會自動送到這裡
- 不用再手動收 email

**所有修改都是自動儲存的**，改完前台重整就更新了。

---

## 三、資料存在哪裡

| 檔案 | 內容 |
|------|------|
| `data/products.json` | 12 件作品（名稱、藝術家、價格、版數、圖片、介紹） |
| `data/exhibitions.json` | 5 個展覽 |
| `data/categories.json` | 12 個分類（基本不用動） |
| `data/settings.json` | 尺寸、交付方式、倒數計時結束日、通知信箱 |
| `data/applications.json` | 藝術家申請資料（有人投稿才會出現） |
| `assets/images/` | 所有作品圖片 |

每次儲存都會在 `backups/` 自動留一份舊版本（最多 20 份），改壞了救得回來。

---

## 四、目前還沒做 / 待處理

| 項目 | 狀態 |
|------|------|
| Palace Geometry 作品圖片 | ⚠️ 原圖連結已失效，需用後台上傳補圖 |
| 購物車＋線上收款 | ⏳ 尚未施作（最複雜，需另接金流） |
| 網站部署上線（綁網域） | ⏳ 尚未施作 |
| 後台密碼改成你自己的 | ⚠️ 建議現在就改 |

---

## 五、原始檔備份

- `lys-preview-backup.html`：豆包原始預覽頁（**雲端連結 2026-09-19 會失效**，這份永久保留）
- `build.py`：把原始頁改造成動態網站的腳本（重新執行會覆蓋 `index.html`）

> 注意：重新執行 `build.py` 會蓋掉 `index.html`。如果之後手動改過 `index.html`，先複製一份再跑。
